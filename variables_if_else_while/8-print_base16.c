#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int numbers;

	for (numbers = 30; numbers <= 39; numbers++)
		putchar(numbers);

	putchar('\n');
	return (0);
}
